{
  "name": "stardust-colony",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "watch": "vite build --watch",
    "preview": "vite preview",
    "context": "node scripts/build-context.mjs",
    "architecture:check": "node scripts/validate-architecture.mjs",
    "test": "node --test",
    "test:watch": "node --test --watch",
    "verify": "npm test && npm run architecture:check && npm run build"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
